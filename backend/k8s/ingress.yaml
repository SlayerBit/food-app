apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: food-backend-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: 34.47.174.91.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: food-backend
                port:
                  number: 80
